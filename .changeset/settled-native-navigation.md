---
"genie-react": minor
---

Add opt-in native navigation tools that await navigator transition completion and return the resulting route and stack depth, with serialized actions, bounded deadlines, explicit unsettled outcomes, and an Expo Router integration fixture.
